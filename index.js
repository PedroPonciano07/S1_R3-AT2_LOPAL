// digite um numero
let numero = prompt("Digite um número:");

// converte o valor digitado
numero = Number(numero);

// Verifica se o número é maior que zero
if (numero > 0) {
  alert("Número positivo.");
} else {
  alert("Número não é positivo.");
}